# Architecture Decision Records

ADRs live in `docs/adr/`. Each decision documents context, the choice taken, and the consequences.

| # | Title |
|---|---|
| 001 | Use `sled` as the default metadata store; RocksDB optional |
| 002 | rustls + ring exclusively (no OpenSSL) |
| 003 | Reed-Solomon erasure coding for v2 distributed mode (proposed) |
